import { DateFilter } from "@/components/ui/date-filter"

// Opens on the Presets tab by default. All four modes (Presets, Custom
// Range, Rolling Window, Fiscal Quarter) live inside the one popover panel.
export default function DateFilterDefault() {
  return <DateFilter />
}
