# PX Shell Registry

Authoritative list of reusable PX page shells extracted from Figma into this repository. Every new list-, master-, form-, or dashboard-style PX screen **must** start from one of these shells before adding page-specific content.

**Shell selection is anatomy-based, never name-based.** A screen's product name (e.g. "Feature Adoption") never determines its shell — its actual anatomy does. A screen dominated by a single tabular list, master surface, or filterable dataset uses `PxListShell`, regardless of what it's named. A screen built around Analytics secondary navigation, KPIs, and charts uses `PxAnalyticsSecondaryNav` composed directly inside `PxMainContainer`, regardless of what it's named — including a screen also named "Feature Adoption." Two screens can share a product name while requiring different shells if their anatomy differs; when in doubt, classify by anatomy first and treat any name-based example list below as illustrative, not authoritative.

| Shell | Purpose | Figma source | Implementation | Example | Visual review | Approved |
| --- | --- | --- | --- | --- | --- | --- |
| `PxListShell` | Two-bar header + left rail + content region + optional 336 px right filter slider. For any PX page whose anatomy is list-driven — a single tabular list, master surface, or filterable dataset (Audiences, Accounts, Segments, Engagements, and any other screen sharing that anatomy, e.g. a table/list-shaped "Feature Adoption" view). | [Shell/MainContainer 3792-8575](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3792-8575), [Page Header 1273-19](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=1273-19), [Filter Panel 20-36](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=20-36) | [`src/patterns/px-list-shell/PxListShell.tsx`](../src/patterns/px-list-shell/PxListShell.tsx) | [`src/pages/engagements-list-example.tsx`](../src/pages/engagements-list-example.tsx) — rendered when the `engagements` rail item is selected | Approved (design owner, 2026-08-27) | Yes |
| `PxCreateEditShell` (Modal / Accordion / Wizard) | Three record-creation/edit tiers: `PxCreateEditShellModal` (≤6 fields), `PxCreateEditShellAccordion` (independent multi-section, full-page), `PxCreateEditShellWizard` (step-dependent, full-page). The Accordion/Wizard tiers compose `PxMainContainer` directly (rail + header stay visible) — not `PxListShell`, so they never inherit its filter-slider slot or list-page semantics. | [Shell/Create · Edit Form 3187-10](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3187-10), [Popup example 3796-2503](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3796-2503), [Accordion example 3796-2504](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3796-2504), [Wizard example 3802-3615](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3802-3615) (Figma layer name says "Accordion" — content/position confirm this is the Wizard tier) | [`src/patterns/px-create-edit-shell/`](../src/patterns/px-create-edit-shell/) | [`src/pages/create-edit-shell-example.tsx`](../src/pages/create-edit-shell-example.tsx) — also embedded live in the Validation Gallery's "Create · Edit Shell" section | Approved (design owner, 2026-08-28) | Yes |
| `PxAnalyticsSecondaryNav` | Fixed-width (312px) full-height Analytics sub-page navigation (4 On-Material Accordion sections × real `<a>`/`<button>` navigation rows with `aria-current="page"` — not Tree/TreeItem), rendered as a sibling of `<main>` inside `PxMainContainer`'s content row — not built on `PxListShell`. Includes a collapse/expand control and a collapsed-rail hover/focus flyout. | [Shell/Analytics/Secondary-Left Navigation 3351-3925](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=3351-3925), defining symbol 3397-2451, [Shell/Nav+SecNav instance 4191-21173](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=4191-21173), [Collapse/expand frame 9576-15005](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=9576-15005), [Collapsed-hover-flyout frame 9576-17226](https://www.figma.com/design/U3D8WMBVFl9LvAZyLHhm24/Prism-V1---ShadCN?node-id=9576-17226) | [`src/patterns/px-analytics-secondary-nav/`](../src/patterns/px-analytics-secondary-nav/), docs page `src/docs/docs/px-analytics-secondary-nav.doc.ts` | [`src/pages/analytics-example.tsx`](../src/pages/analytics-example.tsx) — rendered when the `analytics` rail item is selected; also embedded live in the Validation Gallery's "Analytics Secondary Navigation" section and in Design System Docs → Navigation | Approved (design owner, 2026-08-29) | Yes |

**`PxMainContainer`** (`src/patterns/px-main-container`) is the shared rail+header+content-row foundation both rows above compose — it is deliberately **not** listed as its own row here, since no screen starts from it directly (see its own README's "Registry status" section for why).

## Design-owner approval history

- **2026-08-27** — `PxListShell` approved as production-safe.
- **2026-08-28** — `PxCreateEditShell` (all three tiers — Modal, Accordion, Wizard) approved as production-safe, after a design review that corrected: Accordion's expanded-row hover scope and 24px subtitle-to-content gap, on-material's lack of a special background/container, and Modal's header separation (no border/shadow), 4px title-to-microcopy gap, microcopy typography/colour tokens, and 272px/184px minimum heights. Verified live: header height 72px (with microcopy) / 56px (without), 24px left/right header padding, both exact. See [`src/components/ui/accordion.tsx`](../src/components/ui/accordion.tsx) and [`src/components/ui/modal.tsx`](../src/components/ui/modal.tsx) for the corrected implementation, and [`src/docs/docs/accordion.doc.ts`](../src/docs/docs/accordion.doc.ts) / [`src/docs/docs/modal.doc.ts`](../src/docs/docs/modal.doc.ts) for the corrected documentation. `PxListShell` was not part of this design review and remains approved as of 2026-08-27.
- **2026-08-29** — `PxAnalyticsSecondaryNav` implemented and MCP-verified against 7 Figma nodes (see [`ai/figma-coverage.json`](./figma-coverage.json)'s `shell-analytics-secondary-nav` entry), but **not** design-owner reviewed — visual review pending. Composed from existing `Accordion`/`AccordionItem`; added one additive, non-breaking prop (`contentPadding`, default `true`) for edge-to-edge content. See [`src/patterns/px-analytics-secondary-nav/README.md`](../src/patterns/px-analytics-secondary-nav/README.md) for the full deviation ledger.
- **2026-08-29 (correction pass)** — corrected a semantic error: rows previously reused `Tree`/`TreeItem` (`role="tree"/"treeitem"`), contradicting Figma's own accessibility spec (`role="menuitem"/"link"` + `aria-current="page"`) and requiring a global, non-opt-in keyboard-focusability change to the shared `Tree` component. Rows now render as real `<a>`/`<button>` navigation elements; `tree.tsx` fully reverted, no other `Tree` consumer affected. Also this pass: added a Design System Docs entry (explicit exception to that registry's general Px*-pattern exclusion — see `src/docs/registry.ts`); resolved the Features section icon (`feature-px`, confirmed via exact vector-path match, wired into `src/assets/icons/24/`); corrected `AccordionItem`'s on-material header padding to the existing `--c-accordion-padding-on-material` token (verified this does not affect the approved `PxCreateEditShellAccordion`, which uses off-material). One deviation remained open, requiring design-owner input: the active-item colour token.
- **2026-08-29 (design-owner decisions applied)** — the design owner reviewed the correction pass above and gave three decisions: (1) the selected-row tint is **accepted as-is** (confirmed token-driven, `--c-tree-branch-selected`, not replaced with primary blue); (2) scrolling is **corrected** — the panel no longer scrolls independently (`overflow-hidden` replaces `overflow-y-auto`), only the content area scrolls, verified zero change to `PxMainContainer`/`PxListShell`/`PxCreateEditShell`/`user-explorer.tsx`; (3) a **new** collapse/expand control was added (not part of the original Analytics Figma frame — the only Figma evidence is a separate, generic chevron icon node, `491:83`), implemented via additive extensions to `AccordionItem` (`trailing` slot) and `IconButton` (`variant="primary"`, `forwardRef`), both backward-compatible and verified against every existing consumer. All prior open deviations are now resolved or accepted; one narrow, explicitly-flagged placement question remains (the collapse button's exact position relative to the "My Reports" row). Still `Pending`/`No` — not approved.
- **2026-08-29 (icon and placement corrected against an authoritative Figma update)** — the design owner reported the collapse control's icon and placement were wrong, then updated the actual Figma frame (`9576:15005`) directly to show both variants. Re-implemented exactly against this frame rather than guessing again: added a required `title` prop and a dedicated Title row (node `9576:16007`, "All Reports" + collapse button) — the earlier placement inside the first accordion's own header was wrong and is reverted (`AccordionItem`'s `trailing` prop fully removed); the collapsed state is now the full 56px icon rail Figma's `Property 1=Collapsed` symbol (`9576:15105`) shows — an expand button plus one icon per section — not the lone button implemented previously. The icon itself now uses the real, permanently-filled Figma asset (`icons/filled/chevron-leftmenu-collapse/expand-filled`, `491:83`/`491:82`) via a new `IconButton` `iconStyle="filled"` prop, replacing the earlier guessed plain chevron + custom `variant="primary"` button colour (also reverted — no longer needed). The placement question flagged in the prior entry is now resolved with certainty. Still `Pending`/`No` — not approved.
- **2026-08-29 (collapsed-rail hover flyout)** — implemented against a design-owner-supplied Figma frame (`9576:17226`, "Collapsed Menu Hover behaviour"): hovering or focusing a collapsed-rail section icon (now a 56×56 cell per the more detailed symbol `9576:16362`, superseding the earlier, simpler collapsed-cell reading) opens a flyout (`9576:17185`) of that section's items; selecting one calls the existing `onSelectItem` callback without expanding the panel. Reused the existing, already-approved `DropdownMenu`/`DropdownMenuItem` (`src/components/ui/dropdown-menu.tsx`, unmodified) rather than a bespoke overlay — its tokens are an exact match for the FlyOut's own. A real keyboard-accessibility bug was found and fixed during live verification: every close path (Escape, outside click, item select, hover/blur-away) was silently undone by Radix's own focus-return-to-trigger behavior reopening the flyout; fixed by watching the `open` prop transition itself (a 300ms suppression window sized to outlast Radix's exit animation) rather than gating inside individual close handlers. Verified live: mouse hover open/close, keyboard Tab-focus open, Escape close with focus return, mutual exclusion between sections, and `ArrowDown`/`Enter` selection and close. Still `Pending`/`No` — not approved.
- **2026-08-29 (design-owner approval recorded)** — the design owner visually verified: the expanded navigation, the selected-row tint, the collapsed variant, the collapse/expand control, the collapsed section icons, the hover/focus flyouts, and item selection from the flyout. `PxAnalyticsSecondaryNav` is now **Visual Review: Approved**, **Approved for AI use: Yes**, approval date **2026-08-29** (see [`ai/figma-coverage.json`](./figma-coverage.json)'s `shell-analytics-secondary-nav` entry: `status: "Approved"`, `fidelityReview: "Complete"`, `visualReview: "Approved"`, `designOwnerApproval: { approved: true, date: "2026-08-29" }`). Evidence chain, for traceability: the original Analytics Figma component (frame `9576:15005`) supplied the main navigation evidence; the collapse/expand requirement was added by the design owner afterward, then matched against an authoritative update to that same frame; frame `9576:17226` supplied the collapsed-menu hover-flyout evidence; node `491:83` supplied the filled-chevron visual reference. Before recording this approval, the scrolling gate was re-verified live against `src/pages/analytics-example.tsx`'s intentionally-overflowing content example: the nav panel (`overflow: hidden`, `scrollHeight === clientHeight`) and the primary rail (`overflow: visible`, not scrollable) both stay fixed; only `[data-slot="analytics-example-content"]` scrolls (`overflow: auto`, `scrollHeight` 3427px vs. `clientHeight` 860px); collapsing the panel measurably expands that content region (484px → 740px); `PxMainContainer`, `PxListShell`, `PxCreateEditShell`, and `src/pages/user-explorer.tsx` all diff empty against `origin/main`. This approval covers `PxAnalyticsSecondaryNav` only — it does not newly approve `DropdownMenu`, `Accordion`, `IconButton`, or any other shared component this pattern composes; their own status is unchanged. All prior dated entries above are preserved unedited as historical record of the review process.

## Legend

- **Visual review**: Pending / In review / Approved. Do not mark Approved without explicit sign-off from the design owner.
- **Approved**: `Yes` means the shell is production-safe; `No` means it may only be used behind the Validation Gallery or as a demo route.

## Adding a new shell

1. Extract the Figma frame using `get_metadata` + `get_design_context` (see `CLAUDE.md`).
2. Reuse existing components (`src/components/ui`) — never recreate an approved component.
3. Place the shell under `src/patterns/<shell-name>/` with `<ShellName>.tsx`, `types.ts`, `index.ts`, and `README.md`.
4. Provide one example page under `src/pages/` proving the shell composes.
5. Add a row here and add a rule to `CLAUDE.md` telling Claude to reuse the shell for that page type.
